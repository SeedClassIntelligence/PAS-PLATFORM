/**
 * `@pas/contracts` — shared type contracts and schemas.
 *
 * A leaf package: it depends on nothing, and everything may depend on it.
 */

export * from './errors/index.js';
export * from './temporal/index.js';
