# `@pas/api`

Synchronous API: requests, authentication, authorization, domain-service invocation. SHALL NOT own canonical business rules beyond invoking domain services.

**Status:** scaffold (PAS-0001). No implementation — populated from Build 01 onward.

## Allowed dependencies

`@pas/domain`, `@pas/contracts`, `@pas/auth`

Per PAS-0001's required architectural dependency direction.
